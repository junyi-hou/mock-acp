[project]
name = "mock-acp"
version = "0.1.0"
description = "A mock ACP server that generate fake responses for testing ACP-related projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-client-protocol>=0.9.0",
    "jupyter>=1.1.1",
    "ruff>=0.14.11",
    "ty>=0.0.11",
]

[tool.uv]
exclude-newer = "1 week"
